{
    "title": "An echo request for testing",
    "decription": "Parameters for a request to the test test_echo JSON-RPC method",
    "required": ["question", "sig"],
    "additionalProperties": false,
    "properties": {
        "question": {
            "type": "string"
        },
        "sig": {
            "$ref": "definitions#/definitions/ethereumSignature" 
        }
    }
}

